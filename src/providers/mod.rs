//! Provider subsystem — re-exported from `zeroclaw-providers`.
//!
//! All provider implementations, auth, multimodal processing, and factory functions
//! live in the `zeroclaw-providers` workspace crate.

pub use zeroclaw_providers::*;
